{
  "name": "nofeeswap-deployment",
  "version": "1.0.0",
  "private": true,
  "description": "NoFeeSwap contract compilation (Foundry) and deployment scripts",
  "scripts": {
    "compile:core": "cd core && forge build --evm-version cancun --via-ir --optimize --optimizer-runs 200",
    "compile:operator": "cd operator && forge build --evm-version cancun --via-ir --optimize --optimizer-runs 200",
    "compile:mocks": "forge build",
    "compile": "npm run compile:core && npm run compile:operator && npm run compile:mocks",
    "extract-abis": "node scripts/extract-abis.js",
    "deploy": "npx ts-node scripts/deploy.ts",
    "build": "npm run compile && npm run extract-abis"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "ethers": "^6.16.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
